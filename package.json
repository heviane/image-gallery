{
  "name": "image-gallery",
  "version": "1.0.0",
  "description": "A simple static image gallery application (HTML/CSS).",
  "scripts": {
    "dev": "live-server pub --port=8080",
    "build": "python src/generator.py",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "gallery",
    "image",
    "html",
    "css"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}